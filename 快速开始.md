# 🚀 5分钟快速编译指南

## 方案一：使用 GitHub Actions 自动编译（最简单）

### 步骤

1. **推送到 GitHub**
   - 注册 GitHub 账号：https://github.com/
   - 创建新仓库，命名为 `ConstructionFineApp`
   - 将所有文件上传到仓库

2. **创建 GitHub Actions 配置**
   - 在仓库中创建 `.github/workflows/build.yml`
   - 内容如下：

```yaml
name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install buildozer
        pip install cython
    
    - name: Build APK
      run: |
        buildozer android debug
    
    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: fineapp-apk
        path: bin/*.apk
```

3. **自动编译**
   - 提交并推送代码
   - GitHub Actions 会自动编译
   - 编译完成后下载 APK 文件

4. **安装到手机**
   - 下载 APK 文件
   - 传输到手机安装

---

## 方案二：本地编译（需要配置环境）

### 快速安装（10分钟）

#### 1. 安装 Python
- 下载：https://www.python.org/downloads/
- 安装时勾选 "Add Python to PATH"

#### 2. 安装依赖
```bash
pip install buildozer cython requests
```

#### 3. 双击编译
- 双击运行：`编译.bat`
- 等待编译完成
- 首次需要下载 SDK，可能需要30分钟

#### 4. 安装 APK
```bash
# 连接手机后运行
buildozer android deploy run
```

---

## 📱 下载 APK 方式

### 方式一：GitHub Actions（推荐）
- 自动编译
- 无需本地环境
- 直接下载 APK

### 方式二：CI.Kivy.org
- 访问：https://ci.kivy.org/
- 上传代码
- 自动编译并下载

### 方式三：在线编译服务
- https://www.onlinegdb.com/
- https://replit.com/

---

## 🔧 环境问题解决方案

### 问题 1：JDK 找不到

**解决：**
1. 下载 JDK 8：https://www.oracle.com/java/technologies/javase8-archive-downloads.html
2. 安装后设置环境变量：
   ```
   JAVA_HOME = C:\Program Files\Java\jdk1.8.0_xxx
   PATH 添加：%JAVA_HOME%\bin
   ```

### 问题 2：Android SDK 下载慢

**解决：**
1. 手动下载 SDK：https://developer.android.com/studio
2. 安装后设置环境变量：
   ```
   ANDROID_HOME = C:\Users\你的用户名\AppData\Local\Android\sdk
   PATH 添加：%ANDROID_HOME%\tools
   PATH 添加：%ANDROID_HOME%\platform-tools
   ```

### 问题 3：NDK 找不到

**解决：**
1. 在 Android Studio 中下载 NDK
2. 设置环境变量：
   ```
   ANDROID_NDK_HOME = C:\Users\你的用户名\AppData\Local\Android\sdk\ndk\23.1.7779620
   ```

---

## 📊 编译时间对比

| 方案 | 首次编译 | 后续编译 | 环境要求 | 推荐度 |
|------|---------|---------|---------|--------|
| GitHub Actions | 30分钟 | 10分钟 | 无 | ⭐⭐⭐⭐⭐ |
| CI.Kivy.org | 20分钟 | 8分钟 | 无 | ⭐⭐⭐⭐ |
| 本地编译 | 60分钟 | 5分钟 | 需要配置 | ⭐⭐⭐ |

---

## ✅ 推荐流程

1. **推送到 GitHub**
2. **创建 GitHub Actions**
3. **自动编译下载 APK**
4. **安装到手机**

**总耗时：** 约 40 分钟（含首次编译）

---

## 📞 获取帮助

遇到问题？
- 查看详细文档：README.md
- Buildozer 官方文档：https://buildozer.readthedocs.io/
- Kivy 社区：https://kivy.org/doc/stable/
