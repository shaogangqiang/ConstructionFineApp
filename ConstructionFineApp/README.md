# 📱 施工现场罚款系统 - 安卓APP

## 🎯 功能特点

✅ 拍照/选择图片
✅ 输入违规事项关键词
✅ 选择罚款金额
✅ AI 自动分析违规情况
✅ 生成违规说明和整改意见
✅ 导出 PDF 罚款单（含照片）
✅ 保存历史记录
✅ 完全离线运行（AI分析需联网）

## 📋 系统要求

### Windows 电脑（用于编译APP）
- Windows 10/11
- Python 3.8+
- JDK 8+
- Android SDK
- Android NDK
- 2GB 可用磁盘空间

### 安卓手机
- Android 5.0 (API 21) 或更高版本
- 摄像头权限
- 存储权限
- 网络连接（用于AI分析）

## 🚀 快速开始

### 方法一：直接使用 Buildozer 编译（推荐）

#### 1. 安装依赖

**安装 Python 3.8+**
```bash
# 下载地址：https://www.python.org/downloads/
# 安装时勾选 "Add Python to PATH"
```

**安装 JDK 8**
```bash
# 下载地址：https://www.oracle.com/java/technologies/javase/javase8-archive-downloads.html
# 安装后配置 JAVA_HOME 环境变量
```

**安装 Buildozer**
```bash
pip install buildozer
```

**安装 Cython**
```bash
pip install cython
```

#### 2. 配置环境变量

在系统环境变量中添加：
```
JAVA_HOME = C:\Program Files\Java\jdk1.8.0_xxx
ANDROID_HOME = C:\Users\你的用户名\AppData\Local\Android\sdk
ANDROID_NDK_HOME = C:\Users\你的用户名\AppData\Local\Android\sdk\ndk\23.1.7779620
```

将以下路径添加到 PATH：
```
%JAVA_HOME%\bin
%ANDROID_HOME%\tools
%ANDROID_HOME%\platform-tools
```

#### 3. 编译 APK

在 `ConstructionFineApp` 文件夹中打开命令提示符：

```bash
cd C:\Users\l\clawd\ConstructionFineApp

# 首次运行会自动下载和配置 Android SDK 和 NDK
buildozer android debug

# 编译成功后，APK 文件位于：
# bin\fineapp-0.1-arm64-v8a-debug.apk
```

**首次编译时间：** 30-60分钟（需要下载SDK和NDK）
**后续编译时间：** 5-10分钟

#### 4. 安装到手机

```bash
# 通过 USB 连接手机并开启 USB 调试
buildozer android deploy run

# 或者手动复制 APK 文件到手机安装
```

### 方法二：使用 Buildozer 在线服务

如果本地编译困难，可以使用在线服务：

1. **访问 GitHub**
   - 将项目推送到 GitHub 仓库

2. **使用 GitHub Actions**
   - 添加 `.github/workflows/build.yml` 配置文件
   - 自动编译并生成 APK

3. **使用 Buildozer CI**
   - 访问：https://ci.kivy.org/
   - 上传代码，自动编译

## 📱 在手机上使用

### 安装 APK

1. 在手机上找到 APK 文件
2. 点击安装
3. 允许安装未知来源的应用
4. 安装完成后打开应用

### 授予权限

首次运行时，应用会请求以下权限：
- 📷 摄像头 - 用于拍照
- 📁 存储读写 - 用于保存图片和PDF
- 🌐 网络 - 用于AI分析

### 使用流程

1. **拍照或选择图片**
   - 点击"📷 拍照"按钮拍照
   - 或点击"📁 选择图片"从相册选择

2. **输入违规事项**
   - 在输入框中填写违规事项关键词
   - 例如：未戴安全帽、未系安全带

3. **选择罚款金额**
   - 点击对应的金额按钮
   - 可选：¥100 / ¥200 / ¥500 / ¥1000 / ¥2000 / ¥5000

4. **AI 分析**
   - 点击"🤖 AI分析并生成罚款单"按钮
   - 等待 AI 分析完成

5. **查看结果**
   - 违规事项
   - 罚款金额
   - 违规说明
   - 整改意见
   - 记录时间

6. **导出 PDF**
   - 点击"📤 导出PDF罚款单"按钮
   - PDF 保存到手机的下载文件夹
   - PDF 包含照片和所有信息

## 🔧 高级功能

### 修改 API Key

编辑 `main.py` 文件，找到以下代码：
```python
API_KEY = 'sk-cd0c0e4340b74d5c8490cb634f08dbe2'
```

替换为你自己的千问 API Key。

### 添加应用图标

1. 准备 512x512 像素的 PNG 图标
2. 重命名为 `icon.png`
3. 放在项目根目录
4. 在 `buildozer.spec` 中取消注释：
```
icon.filename = %(source.dir)s/data/icon.png
```

### 修改应用名称

在 `buildozer.spec` 中修改：
```ini
title = 你的应用名称
package.name = yourappname
package.domain = com.yourdomain
```

## 📦 文件说明

```
ConstructionFineApp/
├── main.py              # 主程序文件
├── buildozer.spec       # Buildozer 配置文件
├── requirements.txt      # Python 依赖
├── README.md            # 本文件
└── bin/                 # 编译后的 APK 文件夹
```

## ⚠️ 常见问题

### Q: 编译失败，提示找不到 JDK

A: 确保已安装 JDK 8，并正确配置 JAVA_HOME 环境变量。

### Q: 下载 Android SDK 很慢

A: 使用国内镜像源，或手动下载 SDK 并配置路径。

### Q: APK 安装失败

A: 确保手机开启了"允许安装未知来源的应用"。

### Q: AI 分析失败

A: 检查手机网络连接，确保 API Key 有效。

### Q: PDF 导出失败

A: 确保应用有存储权限，检查下载文件夹是否可写。

### Q: 摄像头无法使用

A: 检查应用是否有摄像头权限，在设置中授予权限。

### Q: 应用闪退

A: 查看手机日志（logcat），查看错误信息。

## 📞 技术支持

### 查看日志

```bash
# 查看编译日志
buildozer android debug 2>&1 | tee build.log

# 查看手机运行日志
adb logcat | grep python
```

### 调试模式

在 `buildozer.spec` 中设置：
```ini
debug = True
```

### 常用命令

```bash
# 查看配置
buildozer init

# 编译 Debug 版本
buildozer android debug

# 编译 Release 版本
buildozer android release

# 部署到手机
buildozer android deploy run

# 清理编译文件
buildozer android clean
```

## 🎓 学习资源

- Kivy 官方文档：https://kivy.org/doc/stable/
- Buildozer 官方文档：https://buildozer.readthedocs.io/
- Python for Android：https://github.com/kivy/python-for-android

## 📝 更新日志

### v1.0.0 (2026-02-04)
- ✅ 首次发布
- ✅ 拍照/选择图片功能
- ✅ AI 分析功能
- ✅ PDF 导出功能
- ✅ 历史记录保存

---

**祝使用愉快！** 🎉
